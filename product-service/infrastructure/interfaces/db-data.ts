export interface StocksTableItem {
    product_id: string;
    count: number;
}

export interface ProductTableItem {
    id?: string;
    description: string;
    price: number;
    title: string;
    count: number;
}

